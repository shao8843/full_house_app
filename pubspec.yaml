name: full_house_app
description: 满堂彩演艺平台.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0
  logging: ^0.11.4
  global_configuration: ^1.6.0
  shared_preferences: ^0.5.12+4
  universal_platform: ^0.1.3
  quiver: ^2.1.3
  flutter_native_splash: ^0.3.0
  artech_meeting:
    path: ./packages/flutter_artech/meeting
    #  artech_chat:
    #      path: ./packages/flutter_artech/flutter_artech_licensed/packages/chat
  flutter_artech:
      path: ./packages/flutter_artech/unified
  artech_payment:
      path: ./packages/flutter_artech/payment
  artech_ui_kit:
      path: ./packages/flutter_artech/flutter_artech_licensed/packages/ui_kit
#  artech_services:
#      path: ./packages/flutter_artech/flutter_artech_licensed/packages/services
  artech_media:
      path: ./packages/flutter_artech/media

  #  artech_agora:
  #    path: ./packages/flutter_artech/flutter_artech_licensed/packages/agora

dev_dependencies:
  flutter_test:
    sdk: flutter
  intl_translation:
    git:
      url: https://github.com/Goxiaoy/intl_translation.git

  flutter_launcher_icons: ^0.8.0
  build_runner: ^1.5.0

# For information on the generic Dart part of this file, see the
dependency_overrides:
  rxdart: ^0.25.0
  intl: ^0.17.0
  flutter_svg: ^0.20.0-nullsafety.3
  xml: ^5.0.0-nullsafety.1
  path_drawing: ^0.5.0-nullsafety.0

flutter_icons:
  image_path: "assets/icons/app_launch_icon.jpg"
  android: true
  ios: true
  web: true
  image_path_ios: "assets/icons/app_launch_icon.jpg"
  image_path_android: "assets/icons/app_launch_icon.jpg"

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/cfg/
    - assets/icons/
    - assets/
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
