query RemoteReminder ($id:ID!) {
    reminder(id: $id){
        id
        name
        body
        dateTime
        related{
            __typename
        }
        user{
            ...UserSimple
        }
        source
        sourceId
    }
}